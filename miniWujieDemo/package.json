{
  "name": "mini-wujie-demo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "main",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n main,react,vue -c blue,cyan,green \"npm --workspace main run dev\" \"npm --workspace react-app run dev\" \"npm --workspace vue-app run dev\"",
    "build": "npm --workspace main run build && npm --workspace react-app run build && npm --workspace vue-app run build",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
